diff --git a/lib/client.js b/lib/client.js
index b0bb430657b4d67a2f197d15201c3d17a1a0f423..c6d8e889eb967a5b1f4e03c878f37fbd83a662ba 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -1660,6 +1660,10 @@ window.__ModuleLoader__.load({
 			rafId = null;
 			/** Guard: after dispose no scheduled work may reinstall anything. */
 			disposed = false;
+			/** Latest local settings write; older settlements must not restore stale snapshots. */
+			writeGeneration = 0;
+			/** Fields whose newest local intent has not reached the authoritative mirror yet. */
+			pendingFields = /* @__PURE__ */ new Set();
 			/**
 			* @param scope - the bound skin-background settings scope.
 			*/
@@ -1672,10 +1676,7 @@ window.__ModuleLoader__.load({
 				this.applyOcclusion();
 				this.syncBlur();
 				scope.subscribe(() => {
-					this.enabledValue = this.readEnabled();
-					this.opacityValue = this.readOpacity();
-					this.blurEmptyValue = this.readBlur(BLUR_EMPTY_FIELD);
-					this.blurContentValue = this.readBlur(BLUR_CONTENT_FIELD);
+					this.refreshFromScope();
 					this.applyOcclusion();
 					this.syncBlur();
 					this.publish();
@@ -1687,7 +1688,7 @@ window.__ModuleLoader__.load({
 				this.applyOcclusion();
 				this.syncBlur();
 				this.publish();
-				this.scope.set("enabled", value);
+				this.persist("enabled", value);
 			}
 			opacity = () => this.opacityValue;
 			blurEmpty = () => this.blurEmptyValue;
@@ -1703,7 +1704,7 @@ window.__ModuleLoader__.load({
 				this.opacityValue = clamped;
 				this.applyOcclusion();
 				this.publish();
-				this.scope.set(OPACITY_FIELD, clamped);
+				this.persist(OPACITY_FIELD, clamped);
 			}
 			setBlurEmpty(value) {
 				const clamped = this.clampBlur(value);
@@ -1711,7 +1712,7 @@ window.__ModuleLoader__.load({
 				this.ensureObserver();
 				this.syncBlur();
 				this.publish();
-				this.scope.set(BLUR_EMPTY_FIELD, clamped);
+				this.persist(BLUR_EMPTY_FIELD, clamped);
 			}
 			setBlurContent(value) {
 				const clamped = this.clampBlur(value);
@@ -1719,10 +1720,37 @@ window.__ModuleLoader__.load({
 				this.ensureObserver();
 				this.syncBlur();
 				this.publish();
-				this.scope.set(BLUR_CONTENT_FIELD, clamped);
+				this.persist(BLUR_CONTENT_FIELD, clamped);
+			}
+			/**
+			* Preserve the newest local intent until the scope's complete serialized
+			* write queue settles. The scope fences responses globally, not per field,
+			* so an earlier field promise may resolve before a later field publishes
+			* the authoritative snapshot.
+			*/
+			persist(field, value) {
+				const generation = ++this.writeGeneration;
+				this.pendingFields.add(field);
+				this.scope.set(field, value).catch(() => {}).finally(() => {
+					if (this.disposed || generation !== this.writeGeneration) return;
+					this.pendingFields.clear();
+					this.refreshFromScope();
+					this.applyOcclusion();
+					this.syncBlur();
+					this.publish();
+				});
+			}
+			/** Refresh only fields that have no newer local intent in flight. */
+			refreshFromScope() {
+				if (!this.pendingFields.has("enabled")) this.enabledValue = this.readEnabled();
+				if (!this.pendingFields.has(OPACITY_FIELD)) this.opacityValue = this.readOpacity();
+				if (!this.pendingFields.has(BLUR_EMPTY_FIELD)) this.blurEmptyValue = this.readBlur(BLUR_EMPTY_FIELD);
+				if (!this.pendingFields.has(BLUR_CONTENT_FIELD)) this.blurContentValue = this.readBlur(BLUR_CONTENT_FIELD);
 			}
 			dispose() {
 				this.disposed = true;
+				this.writeGeneration += 1;
+				this.pendingFields.clear();
 				if (this.rafId !== null) {
 					cancelAnimationFrame(this.rafId);
 					this.rafId = null;
@@ -2333,6 +2361,7 @@ window.__ModuleLoader__.load({
 				doc.head.appendChild(link);
 			}));
 			let latestRequest = 0;
+			let disposed = false;
 			let currentActivation = null;
 			const initialSkinId = doc.documentElement?.getAttribute("data-dsh-skin") || null;
 			let active = initialSkinId;
@@ -2480,7 +2509,8 @@ window.__ModuleLoader__.load({
 					body: JSON.stringify({ active: id })
 				});
 			}
-			async function switchInternal(id, entry, shouldPersist) {
+			async function switchInternal(id, entry, shouldCommit, shouldPersist = shouldCommit) {
+				if (disposed) return active;
 				const seq = ++latestRequest;
 				const activation = ledger.beginActivation();
 				try {
@@ -2504,7 +2534,7 @@ window.__ModuleLoader__.load({
 					currentActivation = activation;
 					active = id;
 					if (entry !== null) lastEntry = entry;
-					if (shouldPersist) {
+					if (shouldCommit) {
 						committed = {
 							id,
 							entry
@@ -2545,6 +2575,9 @@ window.__ModuleLoader__.load({
 				async switchTo(id, entry) {
 					return await switchInternal(id, entry, true);
 				},
+				async adopt(id, entry) {
+					return await switchInternal(id, entry, true, false);
+				},
 				async tryOn(id, entry) {
 					return await switchInternal(id, entry, false);
 				},
@@ -2567,6 +2600,7 @@ window.__ModuleLoader__.load({
 					return await switchInternal(id, id === null ? null : lastEntry, false);
 				},
 				shutdown() {
+					disposed = true;
 					latestRequest += 1;
 					if (currentActivation !== null) {
 						ledger.disposeActivation(currentActivation);
@@ -2608,11 +2642,25 @@ window.__ModuleLoader__.load({
 			const doc = options.doc ?? document;
 			const apiBase = options.apiBase ?? "/api/skin-center/v2";
 			const fetchImpl = options.fetchImpl ?? fetch.bind(doc.defaultView);
+			const Channel = doc.defaultView?.BroadcastChannel;
+			const selectionChannel = Channel ? new Channel("dsh-skin-selection-v1") : null;
+			let stopped = false;
+			let removeAppliedListener = () => {};
 			const controller = createSkinController({
 				doc,
 				ledger: createEffectLedger(),
 				apiBase,
 				fetchImpl,
+				async persist(id) {
+					if (stopped) return;
+					const response = await fetchImpl(`${apiBase}/active`, {
+						method: "POST",
+						headers: { "content-type": "application/json" },
+						body: JSON.stringify({ active: id })
+					});
+					if (!response.ok || (await response.json()).ok !== true) throw new Error("skin selection was not saved");
+					if (!stopped) selectionChannel?.postMessage({ active: id });
+				},
 				suppressBackgroundMedia: options.suppressBackgroundMedia,
 				onError: (message, error) => {
 					console.error(`[skin-center] ${message}`, error);
@@ -2627,9 +2675,11 @@ window.__ModuleLoader__.load({
 				for (const listener of listeners) listener();
 			};
 			async function refreshCatalog() {
+				if (stopped) return;
 				const res = await fetchImpl(`${apiBase}/catalog`);
 				if (!res.ok) throw new Error(`catalog fetch -> ${res.status}`);
 				const payload = await res.json();
+				if (stopped) return;
 				catalog = payload.skins ?? [];
 				diagnostics = payload.diagnostics ?? [];
 				emit();
@@ -2652,10 +2702,27 @@ window.__ModuleLoader__.load({
 					};
 				},
 				shutdown() {
+					if (stopped) return;
+					stopped = true;
+					if (selectionChannel) selectionChannel.onmessage = null;
+					selectionChannel?.close();
+					removeAppliedListener();
+					listeners.clear();
+					if (doc.defaultView?.__skinRuntime === store) delete doc.defaultView.__skinRuntime;
 					adapter.stop();
 					controller.shutdown();
 				}
 			};
+			if (selectionChannel) selectionChannel.onmessage = async ({ data }) => {
+				if (stopped || !data || !(data.active === null || typeof data.active === "string")) return;
+				try {
+					if (catalog === null) await refreshCatalog();
+					if (stopped) return;
+					const entry = data.active === null ? null : store.find(data.active);
+					if (data.active !== null && entry === null) return;
+					await controller.adopt(data.active, entry);
+				} catch (error) { console.error("[skin-center] cross-window selection failed", error); }
+			};
 			{
 				const root = doc.defaultView;
 				root.__skinRuntime = store;
@@ -2663,22 +2730,39 @@ window.__ModuleLoader__.load({
 			(async () => {
 				try {
 					await refreshCatalog();
+					if (stopped) return;
 					let active = doc.documentElement?.getAttribute("data-dsh-skin") || null;
 					if (!active) {
 						const payload = await (await fetchImpl(`${apiBase}/active`)).json();
 						active = payload.ok && typeof payload.active === "string" ? payload.active : null;
 					}
-					if (active === null) return;
+					if (stopped || active === null) return;
 					const entry = store.find(active);
 					if (entry === null) {
 						await controller.switchTo(null, null);
 						return;
 					}
-					await controller.switchTo(active, entry);
+					if (!stopped) await controller.adopt(active, entry);
 				} catch {
-					await controller.switchTo(null, null).catch(() => {});
+					if (!stopped) await controller.switchTo(null, null).catch(() => {});
 				}
 			})();
+			if (doc.defaultView) {
+				const win = doc.defaultView;
+				const onSkinApplied = (event) => {
+					if (stopped) return;
+					const detail = event.detail;
+					if (detail && typeof detail.id === "string") refreshCatalog().then(async () => {
+						if (stopped) return;
+						const entry = store.find(detail.id);
+						if (entry !== null) await controller.switchTo(detail.id, entry);
+					}).catch((error) => {
+						if (!stopped) console.error("[skin-center] applied selection failed", error);
+					});
+				};
+				win.addEventListener("dsh-skin-applied", onSkinApplied);
+				removeAppliedListener = () => win.removeEventListener("dsh-skin-applied", onSkinApplied);
+			}
 			return store;
 		}
 		//#endregion
diff --git a/lib/index.js b/lib/index.js
index 959216d1d7d13e6b87c1b010271f55577fe3b119..bbce336ad329c3bd0e9bfccd7a9849b992399206 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1,4 +1,3 @@
-import { installSettingsSection, settingsNamespace } from "@deepseek-ai/dsh-settings";
 import z from "schemastery";
 import { chmodSync, cpSync, createReadStream, existsSync, lstatSync, mkdirSync, mkdtempSync, readFileSync, readdirSync, realpathSync, renameSync, rmSync, statSync, writeFileSync } from "node:fs";
 import { basename, dirname, extname, join, resolve, sep } from "node:path";
@@ -7223,7 +7222,7 @@ const inject = ["webServer"];
 * skin center. The browser half spells the same string so it can bind the
 * scope without depending on this Host package.
 */
-const SKIN_BACKGROUND_NAMESPACE = settingsNamespace("skin-background");
+const SKIN_BACKGROUND_NAMESPACE = "skin-background";
 /**
 * Runtime schema for SkinBackgroundConfig. Persists the master switch
 * (`enabled`) alongside the background strength fields.
@@ -7240,7 +7239,7 @@ const SkinBackgroundConfigSchema = z.object({
 * persists the selection here; the host half reads weLibraryDirs to extend
 * the library scan beyond the auto-detected Steam folders.
 */
-const SKIN_WALLPAPER_NAMESPACE = settingsNamespace("skin-wallpaper");
+const SKIN_WALLPAPER_NAMESPACE = "skin-wallpaper";
 /** Runtime schema for SkinWallpaperConfig. */
 const SkinWallpaperConfigSchema = z.object({
 	enabled: z.boolean().default(true),
@@ -7266,16 +7265,18 @@ const SkinWallpaperConfigSchema = z.object({
 */
 const apply = mountOnce("@linxin666/dsh-client-ui-skin-center", applyImpl);
 function applyImpl(ctx) {
-	installSettingsSection(ctx, SKIN_BACKGROUND_NAMESPACE, SkinBackgroundConfigSchema, {}, {
-		setSource: () => {},
-		onChange: () => {}
-	});
 	let wallpaperSource = () => ({});
-	installSettingsSection(ctx, SKIN_WALLPAPER_NAMESPACE, SkinWallpaperConfigSchema, {}, {
-		setSource: (source) => {
-			wallpaperSource = source;
-		},
-		onChange: () => {}
+	ctx.inject(["settings"], (settingsCtx) => {
+		settingsCtx.settings.installSection(ctx, SKIN_BACKGROUND_NAMESPACE, SkinBackgroundConfigSchema, {}, {
+			setSource: () => {},
+			onChange: () => {}
+		});
+		settingsCtx.settings.installSection(ctx, SKIN_WALLPAPER_NAMESPACE, SkinWallpaperConfigSchema, {}, {
+			setSource: (source) => {
+				wallpaperSource = source;
+			},
+			onChange: () => {}
+		});
 	});
 	const routes = [...makeSkinCenterV2Routes(), ...makeWeRoutes({
 		getConfig: () => wallpaperSource(),
diff --git a/package.json b/package.json
index bcd10443944eebb4146c75b2d216296b2377ac8f..d9cb8e071e39ea66933a4131905f22edeff1854d 100644
--- a/package.json
+++ b/package.json
@@ -16,7 +16,9 @@
     },
     "client": {
       "inject": [
-        "@deepseek-ai/dsh-client-runtime",
+        "@deepseek-ai/dsh-api-remotes",
+        "@deepseek-ai/dsh-client-connection",
+        "@deepseek-ai/dsh-client-ui-settings",
         "@deepseek-ai/dsh-client-locale",
         "@deepseek-ai/dsh-client-ui-theme"
       ],
@@ -39,7 +41,6 @@
     "vitest": "^4.1.8",
     "@deepseek-ai/cordis": "^4.0.1",
     "@deepseek-ai/dsh-client-locale": "^0.1.0-rc.8",
-    "@deepseek-ai/dsh-client-runtime": "^0.1.0-rc.8",
     "@deepseek-ai/dsh-client-ui-settings": "^0.1.0-rc.8",
     "@deepseek-ai/dsh-client-ui-slots": "^0.1.0-rc.8",
     "@deepseek-ai/dsh-client-ui-theme": "^0.1.0-rc.8",
